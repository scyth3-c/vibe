{
  "name": "vermell",
  "version": "1.0.0",
  "description": "User-friendly and compact C++ Web Framework",
  "scripts": {
    "build": "cmake -DTESTING=ON -S . -B build/ && cmake --build build",
    "test": "ctest --test-dir build",
    "dev:build": "cmake -S . -B build/ && cmake --build build",
    "dev:run": "cmake -S . -B build/ && cmake --build build && ./build/vermellx"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/vermellcc/core.git"
  },
  "bugs": {
    "url": "https://github.com/vermellcc/core/issues"
  },
  "keywords": [
    "http",
    "non-blocking-io",
    "web",
    "c++",
    "cpp20"
  ],
  "license": "MIT",
  "private": false
}
