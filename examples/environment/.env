# Example .env — vibe loads the .env next to the executable automatically.
# Syntax: KEY=VALUE, # comments, optional "export " prefix, optional quotes.

APP_NAME=vibe-environment-demo
PORT=9000
API_TOKEN="secret-token-123"
DEBUG=true
MAX_CONN=250

export EXTRA_KEY='quoted value'   # trailing comments are stripped
